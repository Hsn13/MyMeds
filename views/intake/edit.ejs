<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Intake Log — MyMeds</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../navbar') %>

  <main class="main-content">
    <div class="page-header">
      <h1>Edit Intake Log</h1>
      <a href="/intake?date=<%= log.date.toISOString().split('T')[0] %>" class="btn btn-secondary">← Back</a>
    </div>

    <div class="form-container">
      <form action="/intake/<%= log._id %>?_method=PUT" method="POST">
        <!-- Medication is fixed (read-only) — can't change which med a log belongs to -->
        <div class="form-group">
          <label>Medication</label>
          <input type="text" value="<%= log.medicationId.name %>" disabled class="input-disabled">
        </div>
        <div class="form-group">
          <label>Date</label>
          <input type="text" value="<%= log.date.toLocaleDateString('en-US') %>" disabled class="input-disabled">
        </div>
        <div class="form-group">
          <label for="status">Status <span class="required">*</span></label>
          <select name="status" id="status" required>
            <option value="taken" <%= log.status === 'taken' ? 'selected' : '' %>>✓ Taken</option>
            <option value="missed" <%= log.status === 'missed' ? 'selected' : '' %>>✗ Missed</option>
            <option value="late" <%= log.status === 'late' ? 'selected' : '' %>>⏰ Late</option>
          </select>
        </div>
        <div class="form-group">
          <label for="notes">Notes</label>
          <textarea name="notes" id="notes" rows="2"><%= log.notes %></textarea>
        </div>
        <div class="form-actions">
          <button type="submit" class="btn btn-primary">Update Log</button>
          <a href="/intake?date=<%= log.date.toISOString().split('T')[0] %>" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </main>
</body>
</html>
